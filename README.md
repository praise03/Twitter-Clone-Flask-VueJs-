# Twitter-Clone-Flask-VueJs-

In client folder:
  run:
    "npm install"
    "npm run dev"

in flask-twitter-api folder:
  run:
    "pip install -r requirements.txt";
    "set FLASK_APP = run.py";
    "flask run'
